<div class="container-fluid">
  <div class="jumbotron jumbo">
    <h1>Welcome to Timeclok</h1>
    <% if session[:user] %>
      <h3>Hello <%= (user = User.find(session[:user])).name %></h3>
      <%= link_to "Signout", session_path(user), method: :delete %>
    <% else %>
      <%= link_to "Register", new_user_path, class: "btn btn-default" %>
      <%= link_to "Login", new_session_path, class: "btn btn-default" %>
    <% end %>
    </div>
  </div>

  <div class="row1">
    <div class="col-xs-12 col-sm-6">
      <h3>Manage your daily timecard</h3>
    </div>
    <div class="col-xs-12 col-sm-6">
      <p>Create timecards for a specified time period. Then you can add days to the timecard. On each day you can enter a start time, stop time, lunch start time, and lunch stop time.</p>
    </div>
  </div>

  <div class="row2">
    <div class="col-xs-12 col-sm-6 clearfix">
      <%= link_to "Manage Timecards", timecards_path, class:"btn btn-primary", title: "View and Manage all timecards" %>
    </div>
  </div>

</div>
