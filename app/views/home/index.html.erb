<div class="container-fluid">

  <div class="jumbotron jumbo">
    <h1>Welcome to Timeclok
    <%= image_tag("time-icon.png", class:"home-time") %>
    </h1>

    <% if current_user %>
      <h3>Hello <%= link_to "#{users_name}", edit_user_path(current_user), class: "edit-user" %>
      <%= link_to "<i class='fa fa-sign-out fa-fw'></i>Logout".html_safe, session_path(session), class: "btn btn-default btn-lg", data: {method: :delete}, title:"Logout" %>
      </h3>
    <% else %>
      <%= link_to '<i class="fa fa-plus fa-fw"></i> Create a New Account'.html_safe, new_user_path, class: "btn btn-default" %>
      <%= link_to '<i class="fa fa-sign-in fa-fw"></i> Log In'.html_safe, new_session_path, class: "btn btn-default" %>
    <% end %>
  </div>


  <div class="row1">
    <div class="col-xs-12 col-sm-6 clearfix">
      <%= link_to "<i class='fa fa-archive'></i> Manage Timecards".html_safe, timecards_path, class: "btn btn-default float-btn btn-lg", title: "View and Manage all timecards" if current_user %>
      <%= link_to '<i class="fa fa-plus fa-fw"></i> Edit Profile'.html_safe, edit_user_path(current_user), class: "btn btn-default float-btn btn-lg", title: "Edit profile settings" if current_user %>
    </div>
    <div class="col-xs-12 col-sm-6">
      <p>Create timecards for a specified time period. Then you can add days to the timecard. On each day you can enter a start time, stop time, lunch start time, and lunch stop time.</p>
    </div>
  </div>
  <div class="row2"></div>

</div>
